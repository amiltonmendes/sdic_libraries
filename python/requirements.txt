# Production dependencies for SDIC Libraries
# Install with: pip install -r requirements.txt
# Or install the package directly: pip install -e .

requests>=2.28.0
pandas>=1.5.0
numpy>=1.24.0
python-dotenv>=1.0.0